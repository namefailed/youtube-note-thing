# archive_internal

Working notes — session plans, handoffs, execution logs, audit findings. This
folder is intentionally untracked (see `.gitignore`); only this README is kept.
Durable design docs live in `plans/`, not here.
